# CHEC Dash Parity Databricks App

This folder contains the Databricks App template for the full Dash parity
runtime. The actual deployable app source is staged from the repository root so
the Dash code remains authoritative in one place.

Use `scripts/stage_phase35_databricks_app.py` to build the deployable app
source directory under `databricks/build/`.
